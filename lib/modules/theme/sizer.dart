import 'package:aplikasi_rw/utils/size_config.dart';

class Sizer {
  static double slicingHeight = 8.96;
  static double slicingWidth = 4.14;
  static double slicingText = 8.96;

  static double gapHorizontal16 =
      (16 / slicingWidth) * SizeConfig.widthMultiplier;
  static double gapHorizontal6 =
      (6 / slicingWidth) * SizeConfig.widthMultiplier;
  static double gapVertical16 =
      (16 / slicingHeight) * SizeConfig.heightMultiplier;
  static double gapVertical12 =
      (12 / slicingHeight) * SizeConfig.heightMultiplier;
  static double gapVertical32 =
      (32 / slicingHeight) * SizeConfig.heightMultiplier;
  static double gapVertical24 =
      (24 / slicingHeight) * SizeConfig.heightMultiplier;
  static double gapVertical36 =
      (36 / slicingHeight) * SizeConfig.heightMultiplier;
  static double gapHorizontal25 =
      (25 / slicingWidth) * SizeConfig.heightMultiplier;
}
