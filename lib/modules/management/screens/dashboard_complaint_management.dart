import 'package:flutter/material.dart';

class DashboardComplaintManagement extends StatelessWidget {
  const DashboardComplaintManagement({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}